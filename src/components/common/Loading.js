// api 호출 중 로딩 표시 컴포넌트
const Loading = () => {
    return (
        <div
        className='flex flex-row w-full h-full rounded-md'>
            로딩중 . . .
        </div>
    )
}

export default Loading